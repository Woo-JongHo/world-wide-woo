---
linear_id: WOO-709
linear_uuid: 7895baee-c19d-4d74-99df-0169bb8882c8
unit_id: Code-012
unit_uuid: b9eeb3fa-7aa0-4eb7-bc27-e6322befcada
record_type: requirement
source_revision: worktree:19bad6c00b2dbb4f8c58fd632eb362aae6d31d8d:dirty
updated_at: 2026-09-07T06:32:03Z
---

# 목적

현재 실행 상태와 관측 범위·신선도를 사실대로 표시하는 Monitor 요구를 기록한다.

# 배경과 결정

이 이슈는 현재 snapshot의 관측 투영이며 historical live 상태를 현재 데이터로 보충하지 않는다. 전용 Monitor Unit이 아직 지정되지 않아 이 초안은 공개 activity capture 경계인 Code-012와 연결하되 전용 Unit 필요 여부를 남은 결정으로 둔다.

2026-09-07 초안 당시 `DevelopmentService`는 Code-012 등록 전 후보였다. 현재 `src/workflows/tui-development/service.ts`의 이름 있는 최상위 class `DevelopmentService`에 ` Code-012`가 선언되어 있고, frontmatter UUID와 traceability-v2 원장이 일치한다.

스냅샷 기준 상태는 `Todo`, parent는 `WOO-675`이며 제목과 보호 메타데이터는 변경하지 않는다.

# 상세 동작

`projectRuntimeMonitor`는 sequence 순 activity에서 활성 request/tool/agent/execution/wait, approval, failure와 completion을 계산하고 최근 semantic event 12개와 sourceActivityIds를 반환한다. RuntimeMonitorView는 상태, request/model/agent/tool, 최근 event, retry/failure를 폭 156 이하로 표시한다.

# 예외

현재 projection에는 freshness/coverage 필드가 없고 `unavailableHistoricalMonitor`는 idle·retry 0·failure 0을 반환한다. RuntimeMonitorView는 endpoint가 하나뿐인 elapsed에 현재 시각을 적용하므로 ‘관측된 완료 시간’과 구분이 필요하다.

# 테스트 방법

`test/runtime-monitor.test.ts`와 `test/observability-views.test.ts`로 idle/running/blocked/failed/completed, event limit, responsive width를 확인한다. stale·partial·historical unavailable fixture와 실제 화면을 추가한다.

# 실제 결과

Monitor 관련 11 pass/0 fail이다. 기본 상태·최근 event·폭 제한은 검증됐다. freshness/coverage 계약 부재와 historical unavailable의 idle/0 투영은 코드에서 확인된 남은 결함이다.

# 남은 항목

freshness·coverage 모델, historical unavailable 표기, elapsed endpoint 의미, 실제 40·80·120열 화면과 전용 Unit 또는 Code-012 연결 결정을 완료해야 한다.
