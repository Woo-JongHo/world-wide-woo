---
linear_id: WOO-726
linear_uuid: 3307923e-ea5b-45e0-ba80-3f920ae39c9a
unit_id: Code-013
unit_uuid: f1ec4224-508e-4f9c-9d67-6c32509d404b
record_type: test
source_revision: worktree:19bad6c00b2dbb4f8c58fd632eb362aae6d31d8d:dirty
updated_at: 2026-09-07T06:32:03Z
---

# 목적

Layout 배치·resize·scroll·focus·overlay 복귀의 검증 절차와 실행 결과를 한곳에 연결한다.

# 배경과 결정

자동 layout frame 검증과 실제 terminal 조작을 분리한다. 콘텐츠 기능의 테스트를 Layout 수락으로 중복 계산하지 않는다.

2026-09-07 초안 당시 `createDashboardLayout`의 Code-013 등록은 미완료였다. 현재 `src/tui/layout/dashboard-layout.ts`의 이름 있는 최상위 function `createDashboardLayout`에 ` Code-013`이 선언되어 있고, 같은 파일의 `ChatScrollView`는 기존 Code-003 identity를 유지한다.

스냅샷 기준 상태는 `Backlog`, parent는 `WOO-680`이며 제목과 보호 메타데이터는 변경하지 않는다.

# 상세 동작

createDashboardLayout의 three-region wide frame, compact ordered viewport, height reachability, row cache, contained scroll과 Workbench view-host 유지가 자동 범위다. 실제 범위는 resize·Unicode·무채색·overlay·draft/focus다.

# 예외

테스트가 객체 보존만 확인하고 실제 focus owner를 확인하지 않는 경우, 또는 단일 크기 snapshot만 있는 경우 미충족으로 남긴다.

# 테스트 방법

`bun test test/dashboard-layout.test.ts test/workbench-shell-policy.test.ts`와 120→80→40→120열, 13↔14행 Native TUI 시나리오를 사용한다. 각 단계 scrollTop·anchor·focus·draft를 기록한다.

# 실제 결과

2026-09-07 자동 실행은 33 pass/0 fail이다. wide/compact 구조와 viewport 독립성은 확인됐지만 실제 resize 왕복과 overlay focus 수락은 별도 근거가 없다.

# 남은 항목

mode 전환 anchor 전용 assertion, 실제 Native TUI resize/overlay/무채색/Unicode 증거, Code-013 등록과 Evidence·revision 연결이 필요하다.
