---
linear_id: WOO-724
linear_uuid: 9050073c-9868-43bd-bd3b-1bea5e107dd3
unit_id: Code-012
unit_uuid: b9eeb3fa-7aa0-4eb7-bc27-e6322befcada
record_type: test
source_revision: worktree:19bad6c00b2dbb4f8c58fd632eb362aae6d31d8d:dirty
updated_at: 2026-09-07T06:32:03Z
---

# 목적

Tracer exact selection·실행 tree·Source·복귀의 자동 및 실제 검증 기록을 한곳에 연결한다.

# 배경과 결정

자동 fixture는 identity와 projection을 빠르게 검증하지만 실제 App Server event 형태와 키보드 탐색을 대신하지 않는다.

2026-09-07 초안 당시 `DevelopmentService`는 Code-012 등록 전 후보였다. 현재 `src/workflows/tui-development/service.ts`의 이름 있는 최상위 class `DevelopmentService`에 ` Code-012`가 선언되어 있고, frontmatter UUID와 traceability-v2 원장이 일치한다.

스냅샷 기준 상태는 `Backlog`, parent는 `WOO-681`이며 제목과 보호 메타데이터는 변경하지 않는다.

# 상세 동작

trace-selection, dplan workflow, delegation tree, DevelopmentService checkpoint, Workbench Source view를 대상으로 activity/thread/turn/item/planItem/coverage와 공개 payload를 대조한다.

# 예외

부분 journal, 동일 itemId, ambiguous association, orphan/retry, 원본 부재, oversize payload를 정상 케이스와 같은 비중으로 기록한다. 미실행은 통과로 쓰지 않는다.

# 테스트 방법

Tracer 자동 묶음 36개와 trace-selection 2개를 실행하고 실제 App Server에서 Agent·Tool·승인·결과 tree→Source→Todo 복귀를 조작한다.

# 실제 결과

자동 검증은 합계 38 pass/0 fail이다. 이는 현재 fixture 범위이며 일반 Tool/MCP/승인 전체 tree와 실제 Native TUI 수락을 뜻하지 않는다.

# 남은 항목

실제 App Server event와 Native 화면 근거, 모든 failure mutation, node keyboard 접근, revision·Evidence 연결 및 Code-012 등록이 필요하다.
