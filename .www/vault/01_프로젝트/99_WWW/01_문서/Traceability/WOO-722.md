---
linear_id: WOO-722
linear_uuid: 537de555-50e3-4fd3-a3db-8aaff563b241
unit_id: Code-011
unit_uuid: 4753d64f-ac84-478a-9f74-090eafb7975f
record_type: test
source_revision: worktree:19bad6c00b2dbb4f8c58fd632eb362aae6d31d8d:dirty
updated_at: 2026-09-07T06:32:03Z
---

# 목적

Todo 계획 투영·세션 저장·실행 결속·실패 복구의 검증 방법과 실제 결과를 한곳에 연결한다.

# 배경과 결정

자동 테스트, fixture, 실제 Native TUI 수락을 별도 사건으로 기록한다. 테스트 존재나 대부분 통과를 제품 수락으로 승격하지 않는다.

2026-09-07 초안 당시 `TodoLedger`는 Code-011 등록 전 후보였다. 현재 `src/system/services/todo-ledger.ts`의 이름 있는 최상위 class `TodoLedger`에 ` Code-011`이 선언되어 있고, frontmatter UUID와 traceability-v2 원장이 일치한다.

스냅샷 기준 상태는 `Backlog`, parent는 `WOO-682`이며 제목과 보호 메타데이터는 변경하지 않는다.

# 상세 동작

대상은 TodoLedger, Native Plan wiring, project-workbench session, WorkspaceTodoView다. identity/source/replay, thread별 Todo.md, model/agent ref, narrow rendering, sync state와 CAS를 완료 조건별로 매핑한다.

# 예외

실제 다중 모델을 실행하지 못한 경우 미실행 blocker로 남긴다. 한 실패를 숨기기 위해 전체 pass 숫자만 기록하지 않고 파일·assertion·관측값을 함께 남긴다.

# 테스트 방법

`bun test test/todo-ledger.test.ts test/native-plan-wiring.test.ts test/project-workbench-session.test.ts test/workbench-views.test.ts`를 기준 명령으로 사용하고 Native TUI 시나리오를 별도 기록한다.

# 실제 결과

2026-09-07 기준 122 tests 중 121 pass, 1 fail이다. 실패는 project-workbench-session의 sync rejection 문구 기대이며 실제 구현은 내부 오류 대신 사용자용 요약을 반환한다.

# 남은 항목

실패를 계약에 맞게 해소한 재실행, 실제 병렬 모델·재개·좁은 폭·CAS 복구 화면, Evidence와 revision 연결이 필요하다.
